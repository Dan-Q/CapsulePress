<%= requested_post['post_title'].strip.upcase %>

<%= requested_post['post_date_gmt'] ? requested_post['post_date_gmt'].to_date.to_s.strip : Date.today.to_s.strip %>

<%= Convert.html_to_gophererb(requested_post['post_content']).strip %>
